<?xml version="1.0" encoding="utf-8"?>
<odoo>
        <record id="view_sale_ordenes_globales_detalle_wizard" model="ir.ui.view">
            <field name="name">sale.ordenes.globales.detalle.wizard.form</field>
            <field name="model">sale.ordenes.globales.detalle.wizard</field>
            <field name="arch" type="xml">
                <form string="Órdenes Globales - Detalle">
                    <group>
                        <field name="partner_id"/>
                        <field name="start_date"/>
                        <field name="end_date"/>
                    </group>
                    <!-- 
                    <group>
                        <field name="pos_config_ids" mode="tree" colspan="4" nolabel="1" />
                    </group> 
                    -->
                    <footer>
                        <button name="generate_report_detalle" string="Imprimir" type="object" class="btn-primary" data-hotkey="q"/>
                        <button string="Cancelar" class="btn-secondary" special="cancel" data-hotkey="z" />
                    </footer>
                </form>
            </field>
        </record>

</odoo>
